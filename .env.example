# Render dashboard: set MONGO_URI (MONGODB_URI also works as a fallback)
MONGO_URI=your_mongodb_connection_string
JWT_SECRET=your_jwt_secret
NODE_ENV=production
PORT=5000
# Frontend origin allowed by CORS (your login page host)
CLIENT_URL=https://finwise-1-v170.onrender.com
