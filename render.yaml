services:
  - type: web
    name: finwise
    runtime: node
    plan: free
    buildCommand: npm install && npm install --prefix server && npm run build
    startCommand: node server/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: CLIENT_URL
        value: https://finwise-knz0.onrender.com
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        sync: false