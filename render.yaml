services:
  - type: web
    name: finwise
    runtime: node
    plan: free
    buildCommand: npm run build --prefix . && npm install --prefix server && npm run build --prefix server 2>/dev/null || true
    startCommand: node server/index.js
    envVars:
      - key: NODE_ENV
        value: production
